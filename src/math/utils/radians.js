// src/math/utils/radians.js

/**
 * Converts an angle from degrees to radians.
 * @param {number} degrees
 * @returns {number}
 */
export function radians(degrees) {
  return degrees * (Math.PI / 180);
}