// src/engine/renderer/DebugRenderer.js

/**
 * Placeholder for a debug renderer that draws bounding boxes, normals, or frame info.
 * Not part of the original code; available for development assistance.
 */
export class DebugRenderer {
  draw(ctx, scale, currentQuat, debugData) {
    // Future implementation
  }
}